location = "local-4"
server_id = 4
num_clients = 0 # Hardcoded number of client connections to wait for
output_filepath = "../benchmarks/logs/local-run/server-4.json"
# Cluster-wide settings
local_deployment = true # use localhost
cluster_name = "local-cluster"
nodes = [1, 2, 3, 4, 5]
initial_leader = 1
#initial_flexible_quorum = { read_quorum_size = 3, write_quorum_size = 3 }
